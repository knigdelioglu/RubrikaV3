# 2026-08-08 — TYMM "Görevi oluştur" görünürlük güvencesi (sessiz tıklama kapatma)

- **Kullanıcı şikâyeti:** Formlar doldurulup "Görevi oluştur"a tıklanınca hiçbir şey olmuyor; listede görev oluşmuyor. Kullanıcı: opencode + STRICT + deepseek-v4-flash free.
- **Kök neden (HEAD + gerçek veri kanıtı):** Eski kodun `missingFields`'ı "Tema veya görev adı" (başlık+tema boş) ve "Geçerli rubrik" (rubrik seçilmemiş) koşullarını içeriyordu → kullanıcı bu alanları boş bırakıp formu "doldurduğunu" sanıyordu → canSubmit=false → buton disabled (opacity 0.7, "mavi/aktif" algısı) → tıklama yutuluyor → "hiçbir şey olmuyor". Yeni kod bu koşulları doğru kaldırdı (backend title/initial_rubric opsiyonel — doğrulandı); kullanıcının gerçek projesinde (11_C sınıfı, gradeLevel 11, performans sırası [1,2]) canSubmit TRUE üretiyor. → Büyük olasılıkla kullanıcı eski (yeniden derlenmemiş) build'i test ediyordu.
- **Düzeltme (opencode):** `buildCreateSubmitBlockedFeedback` saf fonksiyonu (buton altı metin + onClick hatası tek kaynak); buton altı görünür `role="alert"` uyarı; onClick guard — canSubmit false ise ErrorBanner (asla sessiz no-op); sequenceQuery.isError → görünür uyarı + "Tekrar dene" (refetch); gradeLevel'sız sınıf → görünür uyarı + Ders Alanı Kurulumu yönlendirmesi; index.css `.performance-submit-reason`; 3 yeni test (toplam 11).
- **Backend:** opencode 1.18.15 + opencode-go/deepseek-v4-flash (free), STRICT (read/edit whitelist + bash/task/webfetch deny, OPENCODE_CONFIG_CONTENT env var enjeksiyonu).
- **Sonuç:** 4 dosya (Page, ui.ts, test, css); typecheck ✓, test 184/184 ✓, lint 0 error/6 warning (yeni yok) ✓, build ✓; src-tauri diff 0.
- **Not:** Kullanıcıya Tauri'yi tamamen kapatıp yeniden başlatma/derleme talimatı verildi; süreç notu: kullanıcı keşfin Hermes yerine ajana bırakılmasını istedi (repo-engineer akışında dikkat).
