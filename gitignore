# ============================================================
# RubrikaV3 — Rust + Tauri + TypeScript
# Bu dosya RubrikaV3 proje klasörünün kökünde ".gitignore"
# adıyla bulunmalıdır.
# ============================================================

# ---------- macOS ----------
.DS_Store
.AppleDouble
.LSOverride
Icon?
._*
.Spotlight-V100
.Trashes

# ---------- Editör / IDE ----------
.idea/
*.swp
*.swo
*~

.vscode/*
!.vscode/extensions.json
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json

# ---------- Node.js / TypeScript ----------
node_modules/
dist/
dist-ssr/
.vite/
.cache/
coverage/
.nyc_output/
*.tsbuildinfo

npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Kilit dosyalarını yoksayma:
# package-lock.json, pnpm-lock.yaml ve yarn.lock Git'e eklenmelidir.

# ---------- Rust / Cargo ----------
target/
src-tauri/target/
**/*.rs.bk

# Cargo.lock uygulama projelerinde Git'e eklenmelidir.

# ---------- Tauri tarafından üretilen dosyalar ----------
src-tauri/gen/schemas/

# Paketleme / kurulum çıktıları
*.app
*.dmg
*.pkg
*.msi
*.AppImage
*.deb
*.rpm
*.apk
*.aab

# ---------- Ortam değişkenleri ve gizli bilgiler ----------
.env
.env.*
!.env.example

*.pem
*.key
*.p12
*.pfx
*.mobileprovision
AuthKey_*.p8

# Yerel ve makineye özel ayarlar
*.local.json
*.local.toml
config.local.*
tauri.conf.local.json

# ---------- Yerel veritabanları ----------
*.db
*.db-shm
*.db-wal
*.sqlite
*.sqlite-shm
*.sqlite-wal
*.sqlite3

# Testlerde özellikle kullanılan küçük fixture veritabanları korunabilir
!fixtures/**/*.db
!fixtures/**/*.sqlite
!fixtures/**/*.sqlite3
!tests/fixtures/**/*.db
!tests/fixtures/**/*.sqlite
!tests/fixtures/**/*.sqlite3
!src-tauri/tests/fixtures/**/*.db
!src-tauri/tests/fixtures/**/*.sqlite
!src-tauri/tests/fixtures/**/*.sqlite3

# ---------- Yerel yapay zekâ modelleri / büyük dosyalar ----------
llm/models/
src-tauri/resources/models/
*.gguf
*.safetensors

# ---------- Geçici çalışma ve kullanıcı verileri ----------
/tmp/
/temp/
/logs/
/backups/
/exports/
/recordings/

*.log
*.tmp
*.temp
*.bak

# ---------- Python yardımcı araçları varsa ----------
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# ---------- Test araçları ----------
playwright-report/
test-results/

# ---------- İşletim sistemi / yerel çalışma kalıntıları ----------
Thumbs.db
Desktop.ini
